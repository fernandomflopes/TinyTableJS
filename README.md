# TinyTableJS

```js
const table = TinyTable("table")
                .addHeader("")
                .addHeader("Title")
                .addHeader("Completed")
                .build()

table.addRow(["Hello","nop"])
table.addRow(["World","ok"])

```

