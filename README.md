# TinyTableJS

```js
const table = TinyTable("tableId")
                .addHeader("Title")
                .addHeader("Completed")
                .build()

table.addRow(["Hello","nop"])
table.addRow(["World","ok"])

```

